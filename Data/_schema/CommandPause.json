
{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "title": "CommandPause",
    "description": "Will pause execution of following commands and queue them up for later usage (either Continue or Flush).",
    "default": {},
    "examples": [
    ],
    "required": [
        "Type"
    ],
    "additionalProperties": false,
    
    "properties": {
        "Type": {"type": "string", "enum": ["Pause"] }
    }
}