
{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "title": "CommandFlush",
    "description": "Will remove commands queued by a Pause-Command and unpause execution for following commands.",
    "default": {},
    "examples": [
    ],
    "required": [
        "Type"
    ],
    "additionalProperties": false,
    
    "properties": {
        "Type": {"type": "string", "enum": ["Flush"] }
    }
}