
{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "title": "CommandBreak",
    "description": "Will prevent all following Commands from being executed. The Break lasts until the next CommandsCollection is executed.",
    "default": {},
    "examples": [
    ],
    "required": [
        "Type"
    ],
    "additionalProperties": false,
    
    "properties": {
        "Type": {"type": "string", "enum": ["Break"] }
    }
}