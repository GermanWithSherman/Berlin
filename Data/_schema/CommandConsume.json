
{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "title": "CommandConsume",
    "description": "Will continue execution of commands queued by a Pause-Command.",
    "default": {},
    "examples": [
    ],
    "required": [
        "Type"
    ],
    "additionalProperties": false,
    
    "properties": {
        "Type": {"type": "string", "enum": ["Consume"] },
        "Hunger":{"type":"integer"},
        "Calories":{"type":"integer"}
    }
}